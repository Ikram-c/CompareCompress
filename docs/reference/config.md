# Configuration reference (`config.yaml`)

*Generated by `tools/gen_config_reference.py` from `config/config.yaml` and `src/core/config.cpp`;
edit those files, not this page.*

The built-in defaults below are compiled into the executable. A `config.yaml` overrides any subset of
them; the first one found is used: the file named with `--config <file>`, `config.yaml` next to the
executable, the per-user file (`~/Library/Application Support/CompressCompare/config.yaml` on macOS,
`~/.config/compresscompare/config.yaml` on Linux; Settings > *edit config.yaml...* creates it), and
`config.yaml` in the working directory.  Maps merge key by key,
scalars and lists replace the default, and the `sites` list merges by name.
Every key is validated at start-up; a mistake stops the program with a message naming the file and the
key. `compresscompare --check-config` validates without opening a window.
See [how-to: change the configuration](../how-to/change-config.md) for the workflow.

| Key | Type | Default | Accepted values | Meaning |
|---|---|---|---|---|
| `schema_version` | integer | `1` | 1 .. 1 | layout version of this file; a build refuses files with another version |
| **`app`** (section) | map | | | window, frame pacing, tooltips, panel geometry |
| `app.name` | text | `CompressCompare` | must not be empty | window title and About text |
| **`app.window`** (section) | map | | | initial size |
| `app.window.width_px` | integer | `1280` | 200 .. 32768 | initial window size at 100 % DPI scale |
| `app.window.height_px` | integer | `760` | 200 .. 32768 | initial window height at 100 % DPI scale |
| `app.ui_scale` | number | `1.0` | 0.25 .. 8 | multiplied with the monitor's DPI scale |
| `app.power_saver` | true / false | `true` |  | sleep between frames when nothing happens |
| `app.auto_visualize` | true / false | `true` |  | recompute as soon as a slot changes |
| `app.compare_at_site_resolution` | true / false | `true` |  | false: upscale the site copy to the original |
| `app.show_loupe` | true / false | `true` |  | corner loupe (before / after / heat at high zoom) |
| `app.show_fps` | true / false | `false` |  | frame-rate and GPU read-out in the status bar |
| **`app.frame_timing`** (section) | map | | | how often the main loop wakes up |
| `app.frame_timing.fast_interval_s` | number | `0.016667` | 0 .. 3600 | frame period while interacting or working (60 Hz) |
| `app.frame_timing.idle_interval_s` | number | `0.25` | 0 .. 3600 | frame period when idle (4 Hz) |
| `app.frame_timing.idle_after_interaction_s` | number | `1.5` | 0 .. 3600 | stay fast this long after the last input |
| `app.frame_timing.iconified_sleep_ms` | integer | `50` | 0 .. 3600000 | sleep per loop iteration while minimised |
| **`app.tooltip`** (section) | map | | | Dear ImGui hover delays |
| `app.tooltip.hover_delay_short_s` | number | `0.25` | 0 .. 3600 | hover time before a short tooltip appears |
| `app.tooltip.hover_delay_normal_s` | number | `0.45` | 0 .. 3600 | hover time before a normal tooltip appears |
| `app.tooltip.hover_stationary_delay_s` | number | `0.12` | 0 .. 3600 | the pointer must rest this long before the delay starts |
| **`app.toast`** (section) | map | | | how long status-bar messages stay |
| `app.toast.short_s` | number | `2.0` | 0 .. 3600 | "3 files dropped" |
| `app.toast.normal_s` | number | `3.0` | 0 .. 3600 | "saved heatmap.png" |
| `app.toast.long_s` | number | `5.0` | 0 .. 3600 | start-up problems |
| **`app.panels`** (section) | map | | | geometry at 100 % scale |
| `app.panels.left_width_px` | number | `360` | 1 .. 65536 | at 100 % scale; also capped by the fraction below |
| `app.panels.left_max_fraction` | number | `0.35` | 0.05 .. 1 | the left panel never takes more than this share of the window |
| `app.panels.right_width_px` | number | `330` | 1 .. 65536 | right panel width at 100 % scale |
| `app.panels.right_max_fraction` | number | `0.30` | 0.05 .. 1 | the right panel never takes more than this share of the window |
| `app.panels.slot_thumb_height_fraction` | number | `0.45` | 0.05 .. 2 | of the panel width |
| `app.panels.slot_thumb_min_px` | number | `90` | 1 .. 65536 | slot thumbnail height floor (scaled) |
| `app.panels.slot_thumb_max_px` | number | `170` | 1 .. 65536 | slot thumbnail height ceiling (scaled) |
| `app.panels.settings_window_width_px` | number | `460` | 1 .. 65536 | initial width of the Settings window |
| `app.panels.about_window_width_px` | number | `560` | 1 .. 65536 | initial width of the Shortcuts & about window |
| `app.panels.plot_height_px` | number | `60` | 1 .. 65536 | height of the PSNR / SSIM plots in the Video section |
| `app.panels.histogram_height_px` | number | `70` | 1 .. 65536 | height of the error histogram in Statistics |
| **`limits`** (section) | map | | | everything that bounds memory, time and loops |
| `limits.max_image_bytes` | integer (bytes) | `209715200` | 1 .. 17179869184 | 200 MiB: still images larger than this are refused |
| `limits.max_download_bytes` | integer (bytes) | `104857600` | 1 .. 17179869184 | 100 MiB: URL downloads (videos use the video cap) |
| `limits.max_image_edge_px` | integer | `16384` | 1 .. 65536 | decoded stills with a longer edge are refused |
| `limits.max_temp_files` | integer | `256` | 1 .. 16777216 | temporary files a session may create |
| **`limits.video`** (section) | map | | | caps applied before and after ffprobe runs |
| `limits.video.max_bytes` | integer (bytes) | `262144000` | 1 .. 17179869184 | 250 MiB: refused before ffprobe runs |
| `limits.video.max_duration_s` | number | `600` | 0 .. 86400 | 10 minutes |
| `limits.video.max_edge_px` | integer | `3840` | 1 .. 65536 | longer edge, either orientation (4K) |
| `limits.sniff_head_bytes` | integer (bytes) | `65536` | 16 .. 16777216 | bytes read to detect a file's type |
| `limits.thumbnail_max_px` | integer | `512` | 1 .. 65536 | slot preview textures are at most this wide |
| `limits.max_site_slots` | integer | `16` | 1 .. 16777216 | upper bound on "+ Add another site" |
| `limits.max_query_chars` | integer | `64` | 1 .. 16777216 | site search box |
| `limits.max_site_name_chars` | integer | `128` | 1 .. 16777216 | longest site name accepted from config.yaml |
| `limits.max_dropped_files` | integer | `64` | 1 .. 16777216 | files accepted from one drag-and-drop |
| `limits.max_url_chars` | integer | `4096` | 1 .. 16777216 | longest URL accepted in the slot text box |
| **`metrics`** (section) | map | | | how the difference metrics and statistics are computed |
| **`metrics.ssim`** (section) | map | | | Wang et al. 2004 window |
| `metrics.ssim.window` | integer | `11` | 3 .. 255 | Gaussian window size in pixels (odd) |
| `metrics.ssim.sigma` | number | `1.5` | 0.1 .. 100 | Gaussian sigma in pixels |
| `metrics.ssim.k1` | number | `0.01` | 1e-06 .. 1 | stabilising constants from Wang et al. 2004 |
| `metrics.ssim.k2` | number | `0.03` | 1e-06 .. 1 | stabilising constant from Wang et al. 2004 |
| `metrics.jnd_delta_e2000` | number | `2.3` | 0 .. 100 | just-noticeable colour difference used by the "visible changes" statistic |
| `metrics.histogram_bins` | integer | `64` | 2 .. 16777216 | bins shown in the statistics panel |
| `metrics.histogram_fine_bins` | integer | `1024` | 2 .. 16777216 | bins used to estimate percentiles |
| `metrics.auto_scale_percentile` | integer | `99` | 1 .. 100 | heat-map auto scale maps this percentile to the top colour |
| `metrics.block_size_px` | integer | `8` | 1 .. 1024 | JPEG block grid for the block view |
| `metrics.psnr_identical_db` | number | `99.0` | 0 .. 1000 | PSNR reported for identical images (would be infinite) |
| `metrics.aspect_mismatch_tolerance` | number | `0.005` | 0 .. 1 | relative aspect-ratio difference that counts as a crop |
| `metrics.custom_table_fit_error_pct` | number | `8.0` | 0 .. 100 | JPEG tables further from IJG tables than this are "custom" |
| **`metrics.default_scale`** (section) | map | | | value mapped to the brightest colour when auto scale is off |
| `metrics.default_scale.abs_diff` | number | `48` | 1e-06 .. 1000000 | 8-bit levels |
| `metrics.default_scale.luma_diff` | number | `32` | 1e-06 .. 1000000 | 8-bit levels |
| `metrics.default_scale.delta_e76` | number | `10` | 1e-06 .. 1000000 | dE units |
| `metrics.default_scale.delta_e2000` | number | `6` | 1e-06 .. 1000000 | dE units |
| `metrics.default_scale.ssim` | number | `0.5` | 1e-06 .. 1000000 | 1 - SSIM |
| `metrics.default_scale.squared_error` | number | `1024` | 1e-06 .. 1000000 | levels squared |
| **`heatmap`** (section) | map | | | initial state of the heat-map panel |
| `heatmap.metric` | one of | `delta_e2000` | abs_diff, luma_diff, delta_e76, delta_e2000, ssim, squared_error | one of abs_diff, luma_diff, delta_e76, delta_e2000, ssim, squared_error |
| `heatmap.colormap` | one of | `inferno` | inferno, viridis, heat, ice, alert, gray | one of inferno, viridis, heat, ice, alert, gray |
| `heatmap.after_mode` | one of | `overlay` | after, heat, overlay | one of after, heat, overlay (before is accepted too) |
| `heatmap.overlay_base` | one of | `site` | original, site | original or site |
| `heatmap.intensity` | number | `0.65` | 0 .. 1 | overlay opacity 0..1 |
| `heatmap.intensity_step` | number | `0.1` | 0.001 .. 1 | [ and ] change the opacity by this much |
| `heatmap.proportional` | true / false | `true` |  | opacity follows the error |
| `heatmap.auto_scale` | true / false | `true` |  | true: the brightest colour goes to metrics.auto_scale_percentile of the map |
| `heatmap.manual_max` | number | `6.0` | 1e-06 .. 1000000 | value mapped to the brightest colour when auto_scale is false |
| `heatmap.manual_max_min_factor` | number | `0.05` | 1e-06 .. 1000000 | slider range as multiples of the metric's default scale |
| `heatmap.manual_max_max_factor` | number | `4.0` | 1e-06 .. 1000000 | upper end of the manual slider, as a multiple of the metric's default scale |
| `heatmap.gamma` | number | `1.0` | 0.01 .. 100 | curve applied to the normalised value (< 1 boosts faint differences) |
| `heatmap.gamma_min` | number | `0.25` | 0.01 .. 100 | lower end of the gamma slider |
| `heatmap.gamma_max` | number | `3.0` | 0.01 .. 100 | upper end of the gamma slider |
| `heatmap.gamma_floor` | number | `0.05` | 0.001 .. 100 | smallest gamma ever sent to the shader |
| `heatmap.threshold` | number | `0.0` | 0 .. 1 | hide everything below this fraction of the scale (0 = show all) |
| `heatmap.block_view` | true / false | `false` |  | average the map over metrics.block_size_px cells |
| `heatmap.log_histogram` | true / false | `true` |  | draw the error histogram on a log scale |
| `heatmap.legend_swatches` | integer | `64` | 2 .. 4096 | colour steps drawn in the legend bar |
| `heatmap.legend_height_px` | number | `14` | 1 .. 65536 | height of the legend bar |
| `heatmap.colormap_preview_steps` | integer | `24` | 2 .. 4096 | colour steps in the colour-map picker swatches |
| `heatmap.colormap_preview_width_px` | number | `60` | 1 .. 65536 | width of the swatches in the colour-map picker |
| **`view`** (section) | map | | | zoom, loupe, split divider and canvas geometry |
| **`view.zoom`** (section) | map | | | wheel / keyboard zoom stepping (darktable-style snapping) |
| `view.zoom.step_below_2x` | number | `1.1` | 1.001 .. 10 | wheel / Ctrl+- step while zoom < 200 % |
| `view.zoom.step_above_2x` | number | `2.0` | 1.001 .. 10 | step while zoom >= 200 % |
| `view.zoom.max` | number | `16.0` | 1 .. 256 | 1600 % |
| `view.zoom.min_fit_fraction` | number | `0.5` | 0.01 .. 1 | cannot zoom out below half of "fit" (or 100 %) |
| `view.zoom.snap_epsilon` | number | `0.00001` | 0 .. 0.5 | zoom values this close to "fit" become "fit" |
| **`view.loupe`** (section) | map | | | the corner magnifier |
| `view.loupe.zoom` | integer | `6` | 1 .. 256 | magnification of the corner loupe |
| `view.loupe.min_zoom` | integer | `2` | 1 .. 256 | lower end of the loupe zoom setting |
| `view.loupe.max_zoom` | integer | `32` | 1 .. 256 | upper end of the loupe zoom setting |
| `view.loupe.size_fraction` | number | `0.18` | 0.01 .. 1 | of the canvas width |
| `view.loupe.min_size_px` | number | `96` | 1 .. 65536 | loupe box edge floor |
| `view.loupe.max_size_px` | number | `200` | 1 .. 65536 | loupe box edge ceiling (scaled) |
| `view.loupe.margin_px` | number | `10` | 0 .. 65536 | distance from the canvas corner |
| `view.loupe.cursor_avoid_px` | number | `20` | 0 .. 65536 | the loupe jumps to the other corner when the pointer gets this close |
| **`view.split`** (section) | map | | | divider hit-testing and rotate handle |
| `view.split.line_hit_px` | number | `7` | 0 .. 65536 | pointer distance that grabs the divider |
| `view.split.handle_hit_px` | number | `14` | 0 .. 65536 | pointer distance that grabs the rotate handle |
| `view.split.handle_radius_fraction` | number | `0.02` | 0 .. 1 | of the pane width |
| `view.split.handle_radius_max_px` | number | `24` | 0 .. 65536 | largest rotate-handle radius |
| `view.pane_gap_px` | number | `4` | 0 .. 65536 | gap between the two panes of Left/Right and Top/Bottom views |
| `view.pan_drag_threshold_px` | number | `2` | 0 .. 65536 | pointer travel before a drag starts panning |
| `view.canvas_min_px` | number | `10` | 1 .. 65536 | smaller canvases are not drawn |
| `view.preview_alpha` | integer | `90` | 0 .. 255 | 0..255 alpha of the site image shown before a comparison exists |
| `view.progress_bar_width_px` | number | `200` | 1 .. 65536 | progress bar on the empty canvas |
| **`fuzzy`** (section) | map | | | site search scoring (Sublime-Text-style subsequence match) |
| `fuzzy.base_score` | integer | `100` | -10000 .. 10000 | score every match starts from |
| `fuzzy.sequential_bonus` | integer | `15` | -10000 .. 10000 | adjacent matches |
| `fuzzy.separator_bonus` | integer | `30` | -10000 .. 10000 | match right after a space, '-', '_', '.', '/', '(' or ',' |
| `fuzzy.camel_bonus` | integer | `30` | -10000 .. 10000 | match on an upper-case letter after a lower-case one |
| `fuzzy.first_letter_bonus` | integer | `15` | -10000 .. 10000 | match at index 0 |
| `fuzzy.leading_letter_penalty` | integer | `-5` | -10000 .. 10000 | per unmatched leading character |
| `fuzzy.max_leading_letter_penalty` | integer | `-15` | -10000 .. 10000 | floor of the accumulated leading-letter penalty |
| `fuzzy.unmatched_letter_penalty` | integer | `-1` | -10000 .. 10000 | per haystack character that was not matched |
| `fuzzy.exact_substring_bonus` | integer | `40` | -10000 .. 10000 | the whole pattern appears contiguously |
| `fuzzy.dropdown_rows` | integer | `8` | 1 .. 1000 | rows visible in the site dropdown |
| `fuzzy.dropdown_min_width_px` | number | `260` | 1 .. 65536 | the site dropdown is at least this wide |
| **`network`** (section) | map | | | URL downloads |
| `network.user_agent` | text | `"Mozilla/5.0 (compatible; CompressCompare/0.2; +https://github.com/)"` | must not be empty | User-Agent header sent with every request |
| `network.accept` | text | `"image/avif,image/webp,image/apng,image/*,video/*,*/*;q=0.8"` | must not be empty | Accept header sent with every request |
| `network.connect_timeout_s` | integer | `15` | 1 .. 3600 | give up connecting after this long |
| `network.low_speed_limit_bytes_per_s` | integer | `1024` | 0 .. 16777216 | abort when slower than this ... |
| `network.low_speed_time_s` | integer | `30` | 0 .. 3600 | ... for this long |
| `network.max_redirects` | integer | `10` | 0 .. 100 | redirects followed before giving up |
| `network.read_chunk_bytes` | integer (bytes) | `65536` | 1 .. 16777216 | bytes read per call from the socket |
| **`network.winhttp`** (section) | map | | | time-outs of the WinHTTP back end (Windows builds without libcurl) |
| `network.winhttp.resolve_timeout_ms` | integer | `15000` | 1 .. 3600000 | DNS time-out (Windows builds without libcurl) |
| `network.winhttp.connect_timeout_ms` | integer | `15000` | 1 .. 3600000 | connect time-out |
| `network.winhttp.send_timeout_ms` | integer | `30000` | 1 .. 3600000 | request send time-out |
| `network.winhttp.receive_timeout_ms` | integer | `60000` | 1 .. 3600000 | response receive time-out |
| **`process`** (section) | map | | | external programs (ffmpeg, ffprobe, dialogs) |
| `process.default_timeout_ms` | integer | `60000` | 1 .. 3600000 | ffmpeg frame extraction and other runs |
| `process.probe_timeout_ms` | integer | `30000` | 1 .. 3600000 | ffprobe runs |
| `process.version_timeout_ms` | integer | `10000` | 1 .. 3600000 | ffmpeg -version at start-up |
| `process.dialog_timeout_ms` | integer | `600000` | 1 .. 3600000 | a file dialog can stay open for 10 minutes |
| `process.stderr_cap_bytes` | integer (bytes) | `1048576` | 1 .. 17179869184 | stderr kept per run (for error messages) |
| `process.probe_stdout_cap_bytes` | integer (bytes) | `1048576` | 1 .. 17179869184 | stdout kept from ffprobe |
| `process.version_stdout_cap_bytes` | integer (bytes) | `65536` | 1 .. 17179869184 | stdout kept from ffmpeg -version and small helpers |
| `process.dialog_stdout_cap_bytes` | integer (bytes) | `65536` | 1 .. 17179869184 | stdout kept from zenity / kdialog / osascript |
| `process.pipe_chunk_bytes` | integer (bytes) | `65536` | 1 .. 16777216 | bytes read per call from a child's pipe |
| `process.frame_cap_slack_bytes` | integer (bytes) | `1024` | 0 .. 17179869184 | allowed bytes beyond w*h*3 when reading a raw frame |
| `process.max_read_iterations` | integer | `1048576` | 1 .. 16777216 | formal upper bound on pipe reads per process |
| **`ffmpeg`** (section) | map | | | locating and driving ffmpeg / ffprobe |
| `ffmpeg.search_subdirs` | list of text | `[ffmpeg, ffmpeg/bin, bin, tools, ../ffmpeg/bin]` | at most 32 entries | relative to the executable |
| `ffmpeg.extra_search_dirs` | list of text | `[/usr/local/bin, /opt/local/bin, /opt/homebrew/bin]` | at most 32 entries | POSIX only (/opt/local = MacPorts) |
| `ffmpeg.scale_filter` | text | `lanczos` | must not be empty | ffmpeg scaler used when a frame is resized on the way out |
| `ffmpeg.seek_end_margin_s` | number | `0.05` | 0 .. 3600 | never seek closer than this to the end of a clip |
| `ffmpeg.fallback_frame_step_s` | number | `0.04` | 1e-06 .. 3600 | frame step used when ffprobe reports no frame rate |
| `ffmpeg.max_still_edge_px` | integer | `16384` | 1 .. 65536 | AVIF/HEIC/TIFF stills decoded through ffmpeg |
| `ffmpeg.sample_points` | integer | `8` | 1 .. 16777216 | "sample quality over time" default |
| `ffmpeg.sample_points_min` | integer | `3` | 1 .. 16777216 | lower end of the sample-count slider |
| `ffmpeg.sample_points_max` | integer | `24` | 1 .. 16777216 | upper end of the sample-count slider |
| **`threading`** (section) | map | | | CPU parallelism |
| `threading.max_workers` | integer | `8` | 1 .. 1024 | OpenCV's thread pool uses at most this many threads (capped by the core count) |
| `threading.fallback_workers` | integer | `2` | 1 .. 1024 | when the core count is unknown |
| `threading.min_rows_per_worker` | integer | `16` | 1 .. 16777216 | parallel_for() granularity (kept for compatibility; the image code uses OpenCV's pool) |
| **`acceleration`** (section) | map | | | OpenCV and OpenCL (GPU) use |
| `acceleration.opencl` | one of | `auto` | auto, on, off | auto: use the GPU when its OpenCL driver passes a start-up self-test; on: use any OpenCL device whose kernels build; off: CPU only |
| `acceleration.opencl_device` | text | `""` |  | OpenCV device selector (OPENCV_OPENCL_DEVICE), e.g. ":GPU:0"; "" = default |
| `acceleration.min_gpu_pixels` | integer | `262144` | 0 .. 4294967296 | images smaller than this stay on the CPU (the upload costs more than it saves) |
| `acceleration.self_test_tolerance` | number | `0.01` | 1e-06 .. 1 | largest accepted GPU-vs-CPU difference per value in the self-test |
| `acceleration.video_backend` | one of | `auto` | auto, opencv, ffmpeg | auto: OpenCV (AVFoundation on macOS) first, then the ffmpeg executable; opencv or ffmpeg: only that one |

## The `sites` list

`sites` is a list of maps with four keys:

| Key | Type | Required | Meaning |
|---|---|---|---|
| `name` | text | yes | Display name; unique (case-insensitive). |
| `aliases` | list of text | no | Search aliases for the site box (`ig`, `insta`, ...); lower-cased on load. At most 64. |
| `hosts` | list of text | no | Host suffixes for URL auto-detection: a host matches `s` when it equals `s` or ends with `.s`. At most 64. |
| `notes` | text | no | What the site typically does to uploads; shown on hover. |

An override entry whose `name` matches a built-in site updates the fields it lists and keeps the
others; a new name is appended to the end of the list. The list is limited to 512 entries. The built-in
sites are listed in [sites.md](sites.md).

## Cross-field rules

Besides the per-key ranges the loader checks that `metrics.ssim.window` is odd,
`metrics.histogram_fine_bins >= metrics.histogram_bins`, `heatmap.gamma` lies within
`[heatmap.gamma_min, heatmap.gamma_max]`, `heatmap.manual_max_min_factor < heatmap.manual_max_max_factor`,
`view.loupe.zoom` lies within `[view.loupe.min_zoom, view.loupe.max_zoom]`,
`view.loupe.min_size_px <= view.loupe.max_size_px`, `app.panels.slot_thumb_min_px <= app.panels.slot_thumb_max_px`,
`ffmpeg.sample_points` lies within `[ffmpeg.sample_points_min, ffmpeg.sample_points_max]` and
`threading.fallback_workers <= threading.max_workers`.

## Constants that are deliberately not configurable

Physical and standards constants stay in the source as named `constexpr` values: the sRGB transfer
curve and D65 matrix, the CIE Lab thresholds, the CIEDE2000 weights, Rec.601 luma, the ITU-T T.81
Annex K quantisation tables, JPEG marker codes, the colour-map anchor colours, OpenGL enumerants and
the cosmetic paddings and colours of the widgets (`src/app/ui_style.h`).
